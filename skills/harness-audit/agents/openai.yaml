interface:
  display_name: "Harness Audit"
  short_description: "Audit agent releases, lifecycles, authority, and effects"
  default_prompt: "Use $harness-audit to pin this agent's evaluated runtime bundle, map authority and lifecycle attack paths, challenge consequential effects with causal proof, and make an impact-tiered launch decision."
